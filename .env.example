# RHTMX Environment Variables Example
# Copy this file to .env and customize with your environment-specific secrets
# This file contains SECRETS - do NOT commit to git
#
# Non-sensitive configuration (port, hot_reload, directories, etc.) goes in rhtmx.toml

# ============================================================================
# SQLite (Default - No configuration needed)
# ============================================================================
# Uses a local SQLite file for data persistence
# DATABASE_URL=sqlite:rhtmx.db
#
# For in-memory SQLite (useful for testing):
# DATABASE_URL=sqlite::memory:

# ============================================================================
# PostgreSQL (Local)
# ============================================================================
# Replace username, password, localhost, and dbname with your values
# DATABASE_URL=postgres://username:password@localhost:5432/rhtmx_db

# ============================================================================
# PostgreSQL (Docker)
# ============================================================================
# If running PostgreSQL in Docker with docker-compose:
# 1. Create docker-compose.yml with PostgreSQL service
# 2. Run: docker-compose up -d
# 3. Use this connection string:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/rhtmx_db

# ============================================================================
# Supabase (PostgreSQL)
# ============================================================================
# Get your Supabase connection string from:
# Dashboard → Your Project → Settings → Database → URI
#
# Replace values from your Supabase dashboard:
# DATABASE_URL=postgres://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres
#
# Example (not real credentials):
# DATABASE_URL=postgres://postgres:AbCdEf123456@db.abc123def456.supabase.co:5432/postgres

