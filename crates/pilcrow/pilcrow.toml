# ./crates/pilcrow/pilcrow.toml
# Pilcrow Configuration File
# This file is optional — Pilcrow works with sensible defaults if missing.

[project]
name = "pilcrow-app"
version = "0.1.0"

[server]
# Server port (default: 3000)
port = 3000
# Server host (default: 127.0.0.1)
host = "127.0.0.1"
